import { Link, useParams } from "react-router-dom";
import { ArrowLeft, Calendar, MapPin, Star, Trophy } from "lucide-react";
import Header from "@/components/Header";
import Footer from "@/components/Footer";
import { useArenaStore } from "@/lib/arenaStore";

const BullProfilePage = () => {
  const { id } = useParams();
  const { data } = useArenaStore();
  const bull = data.bulls.find((item) => item.id === Number(id));
  if (!bull) return <div className="min-h-screen bg-background flex items-center justify-center text-muted-foreground">Touro não encontrado.</div>;
  return <div className="min-h-screen bg-background"><Header /><main className="pt-20 pb-20"><div className="relative h-[50vh] overflow-hidden"><img src={bull.image || "/placeholder.svg"} alt={bull.name} className="w-full h-full object-cover" /><div className="absolute inset-0 bg-gradient-to-t from-background via-background/60 to-transparent" /><div className="absolute bottom-0 left-0 right-0 p-8"><div className="container mx-auto"><Link to="/touros" className="inline-flex items-center gap-2 text-primary text-sm mb-4 hover:underline"><ArrowLeft className="w-4 h-4" />Voltar para Touros</Link><h1 className="text-5xl md:text-6xl font-semibold uppercase text-gradient-gold">{bull.name}</h1><div className="flex flex-wrap items-center gap-4 mt-3"><div className="flex items-center gap-1"><Star className="w-5 h-5 text-primary fill-primary" /><span className="text-primary text-2xl font-bold">{bull.score}</span></div><span className="text-foreground">{bull.company}</span><div className="flex items-center gap-1 text-muted-foreground"><MapPin className="w-4 h-4" />{bull.city}</div></div></div></div></div><div className="container mx-auto px-4 mt-10 grid lg:grid-cols-3 gap-8"><div className="space-y-6"><div className="bg-card border border-gold rounded-lg p-6 space-y-3">{[{ label: "Idade", value: `${bull.age} anos` }, { label: "Peso", value: `${bull.weight} kg` }, { label: "Tropeiro", value: bull.tropeiro }, { label: "Companhia", value: bull.company }, { label: "Eventos", value: String(bull.events) }, { label: "Vitórias", value: String(bull.wins) }].map((item) => <div key={item.label} className="flex justify-between border-b border-border pb-2"><span className="text-muted-foreground text-sm">{item.label}</span><span className="text-sm">{item.value}</span></div>)}</div><div className="grid grid-cols-2 gap-4"><div className="text-center p-4 bg-card border border-gold rounded-lg"><Trophy className="w-6 h-6 text-primary mx-auto mb-2" /><p className="text-2xl font-bold">{bull.wins}</p><p className="text-xs text-muted-foreground">Vitórias</p></div><div className="text-center p-4 bg-card border border-gold rounded-lg"><Calendar className="w-6 h-6 text-primary mx-auto mb-2" /><p className="text-2xl font-bold">{bull.events}</p><p className="text-xs text-muted-foreground">Eventos</p></div></div></div><div className="lg:col-span-2 bg-card border border-gold rounded-lg p-6"><h3 className="text-lg uppercase text-primary mb-6">Histórico de avaliações</h3>{bull.history.length ? <div className="space-y-4">{bull.history.map((h, i) => <div key={i} className="flex items-center justify-between p-4 bg-muted rounded-lg"><div><p className="font-semibold text-foreground">{h.event}</p><p className="text-sm text-muted-foreground">{h.date}</p></div><div className="flex items-center gap-1"><Star className="w-4 h-4 text-primary fill-primary" /><span className="text-xl font-bold text-primary">{h.score}</span></div></div>)}</div> : <p className="text-muted-foreground">Nenhum histórico disponível.</p>}</div></div></main><Footer /></div>;
};

export default BullProfilePage;
